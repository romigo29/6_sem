services:
  mssql-celebrities:
    image: mcr.microsoft.com/mssql/server:2019-latest
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: "${DB_PASSWORD}"
      MSSQL_PID: "Developer"
    ports:
      - "10433:1433"
    volumes:
      - mssql_celebrities_data:/var/opt/mssql

  tdwa06-01:
    build: .
    env_file:
      - .env
    depends_on:
      - mssql-celebrities

  nginx:
    image: nginx:latest
    depends_on:
      - tdwa06-01
      - mssql-celebrities
    ports:
      - "8080:80"
      - "11433:1433"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro

volumes:
  mssql_celebrities_data:
